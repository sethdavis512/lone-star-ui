---
"lone-star-ui": patch
---

Dependency maintenance: bump `tailwind-merge` to `^3.6.0` and widen the `typescript` peer range to `^5 || ^6` so consumers on TypeScript 6 are supported. Dev tooling (Storybook 10.4, Vitest/Playwright, Tailwind 4.3) updated to latest; these do not affect consumers.
